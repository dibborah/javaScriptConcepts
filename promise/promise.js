// Syntax 

let promise = new Promise((resolve, reject)=>{

})

// Defination

// Promise is a Javascript object which promises an execution of a code.
// The code either executes or fail .In both the cases the subscriber will be notified.